import Person, greet from 'examples/person'

val people = [ Person(firstName: 'Ken', age: 26), Person(firstName: 'Meg', age: 25) ]

val greetings = greet(people[0], 'Ken') + ', and ' + greet(p: people[1], name: 'Meg', greeting: 'welcome')
